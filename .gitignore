node_modules/
.next/
.env.local
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal
.DS_Store
.vercel
*-log.txt
env-list.txt
_to_delete/

# Claude Code local settings (machine-specific)
.claude/settings.local.json
.scratch/
data/

# TypeScript's incremental build cache. Regenerated by every `typecheck` and
# `build`, so tracking it put a meaningless diff in 33 commits and guaranteed a
# conflict on any rebase.
*.tsbuildinfo
